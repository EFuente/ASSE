version: 2.0
jobs:
 build:
   machine:
     enabled: true
     image:   circleci/classic:Latest
   steps:
     - run:
         name: Update npm
         command: |
                  sudo npm install -g npm@latest
     - run:
         name: Install lftp
         command: |
                  install -g lftp
           
