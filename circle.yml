## Customize dependencies
dependencies:
  pre:
    - npm install lftp # install from a different package manager
    
deployment:
  staging:
    branch: master
    commands:
      - lftp -e 'put /test.efo -O /public_html; bye' -u $FTP_TEST_U $FTP_TEST_URL
