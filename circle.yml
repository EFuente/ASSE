## Customize dependencies
dependencies:
  pre:
    - sudo apt-get install lftp
    
deployment:
  staging:
    branch: master
    commands:
      #- lftp -u $FTP_TEST_U -p 21 $FTP_TEST_URL
      - ls
      - lftp -e 'rcd /public_html; put /test.efo; bye' -u $FTP_TEST_U $FTP_TEST_URL
