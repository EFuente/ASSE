machine:
    - sudo apt-get install lftp

deployment:
    production:
        branch: master
        echo $FTP_TEST_URL
        #lftp -e 'put /test.efo -O /public_html; bye' -u $FTP_TEST_U $FTP_TEST_URL
